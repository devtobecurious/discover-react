import React from 'react';

const person2 = (props) => {
    return (
        <div>
                <b>2</b>
            <p>Hi I'm {props.name}, with age : {props.age}</p>
        </div>
    );
};

export default person2;