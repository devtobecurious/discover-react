import React, { Component } from 'react';

const People1 = (props) => {
    return (
        <div>
            <b>People !</b>
            <span>{props.name}</span>
        </div>
    );
}

export default People1;