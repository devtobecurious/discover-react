import * as store from './state';

const counterReducer = (state = store.state, action) => {
    if (action.type === 'login') {
        
    }

    if (action.type === 'logout') {

    }

    return state;
};